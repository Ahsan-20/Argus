# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32

# Config / validation
pydantic>=2.9
pydantic-settings>=2.5

# Database (Supabase Postgres via psycopg 3)
sqlalchemy>=2.0
psycopg[binary]>=3.2

# Fetching + readable extraction
httpx>=0.27
trafilatura>=1.12

# LLM providers
google-genai>=0.3
groq>=0.11

# Email: sent via SMTP using the stdlib smtplib, no package needed.

# Env loading for local dev
python-dotenv>=1.0
